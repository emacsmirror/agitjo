* AGitjo

AGitjo is a [[https://www.gnu.org/software/emacs/][GNU Emacs]] package that extends Magit with a new menu for AGit-Flow
operations, to make them more convenient for users.  The AGit workflow enables
users to create and edit pull requests using just the "git push" command.  This
package is intended specifically for use with Forgejo-based (e.g. Codeberg)
repositories.

See [[https://forgejo.org/docs/latest/user/agit-support/][Forgejo's documentation about AGit-Flow]] to learn more.

[[file:images/menu.png]]

** Installation

Make sure the following dependencies are satisfied before using this package:
- emacs 30.1.
- magit 4.3.8.
- markdown-mode 2.7.
- transient 0.9.1.


This package can be installed with Guix, using the =guix.scm= file in this
repository.  For example, to install to the user profile, one may run the
following in this repository's root directory:
#+begin_src emacs-lisp
  guix package --install-from-file=guix.scm
#+end_src

** Usage

Run the following to load AGitjo:
#+begin_src emacs-lisp
  (use-package agitjo)
#+end_src

An average workflow might look like the following:
1. Write and commit some code, and eventually decide it's ready for a pull
   request.
2. Open Agitjo's menu inside the repository with =M-x agitjo-push= or by
   inputting the =#= key inside a Magit status buffer.
3. Set options like the title (=-t=) and the session identifier (=-s=).
4. Invoke one of the push commands to begin a git-push operation.
5. When creating a new PR (i.e., the force-push option =-f= is disabled), a
   dedicated buffer will open for editing the PR description before pushing.
6. After a satisfactory description is written, confirm that it is completed and
   send it with =C-c C-c=.  This process can also be aborted with =C-c C-k=.
7. Visit the created pull request's link (=V=) to inspect it and make any
   additional adjustments, if needed.
8. Repeat steps 1-4 with the force-push option (=-f=) enabled to push changes to
   existing PRs associated with the session/topic identifier (=-s=).
